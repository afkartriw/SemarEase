/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['placehold.co'], // Mengizinkan semua path dari domain ini secara otomatis
    },
  };
  
  export default nextConfig;
  